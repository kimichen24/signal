"""Run the 100-item human eval workflow.

Implemented in prompts/05_EVAL_POLISH.md. Samples 100 issues for human
labelling of category / surface / platform / severity and compares against AI
outputs. Results are recorded honestly — never preset (docs/EVAL_PLAN.md).
"""


def main() -> None:
    raise NotImplementedError(
        "evaluate is delivered with prompts/05_EVAL_POLISH.md"
    )


if __name__ == "__main__":
    main()
