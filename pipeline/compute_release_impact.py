"""Compare feedback structure before/after release events.

Implemented in prompts/04_RELEASES_ACTIONS.md. Default window ±7 days.
Reports correlation only — the output must never claim a release caused an
issue (AGENTS.md §2.3).
"""


def main() -> None:
    raise NotImplementedError(
        "compute_release_impact is delivered with prompts/04_RELEASES_ACTIONS.md"
    )


if __name__ == "__main__":
    main()
