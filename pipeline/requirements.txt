# Runtime (full install happens with Prompt 01+; pytest is enough for the bootstrap config tests)
httpx>=0.27
openai>=1.40
psycopg[binary]>=3.2
python-dotenv>=1.0
jsonschema>=4.23

# Dev / test
pytest>=8.3
